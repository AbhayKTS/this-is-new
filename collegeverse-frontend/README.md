# CollegeVerse — Frontend (Next.js)

Quick start (PowerShell on Windows):

1. Install dependencies

```powershell
cd T:\collegeverse\collegeverse-frontend
npm.cmd install
```

2. Run dev server

```powershell
npm.cmd run dev
```

Open http://localhost:3000

Notes:
- Replace values in `.env.local` (or create one from `.env.local.example`) before connecting to your backend or Supabase.
- Use `npm.cmd` in PowerShell to avoid execution-policy issues.
